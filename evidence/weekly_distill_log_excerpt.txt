═══════════════════════════════════════════════════════════════════════════
evidence/weekly_distill_log_excerpt.txt — MOVED / 移転しました

  The log excerpt is now at:

      cookbook/author/evidence/weekly_distill_log_excerpt.txt

  THE ARTIFACT ITSELF — the real run log, non-zero exit and all — LIVES ON
  THE SHELF, NOT HERE. This file is a signpost kept so the old path keeps
  resolving; it contains no evidence. Nothing in the excerpt was edited by
  the move: the run log is quoted verbatim there, exactly as it was here.

  実ログの現物は cookbook 側にあります。ここにあるのは案内だけで、
  証拠は1行も入っていません。移動で本文は書き換えていません
  （実ログ内に書かれた当時のパス表記も、証拠性を壊さないため無変更）。

  Why it moved: the repository is two layers — core (mechanism: scaffolding,
  scripts, empty forms, the acceptance gate) and cookbook/ (samples: somebody's
  filled-in content). Excerpts from the author's live instance are samples.

      boundary          docs/layers.md
      old-path lookup   docs/path-migrations.md
      the shelf         cookbook/README.md
═══════════════════════════════════════════════════════════════════════════
