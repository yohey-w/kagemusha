# decisions_journal — 判断台帳（L2・追記専用イベントログ）

> **判断蒸留の材料（L2）。** 裁定・訂正・却下・温度を、起きたそばから1件書く。
> `decisions.md`（SSOT）とは別物: あちらは「いま何が有効か」、こちらは「どう判断する人か」。
> 主な読者はエージェントと週次蒸留。ID・topic・マーカーの書式を崩さないこと（正規表現で引かれる）。
> 詳しくは [`../docs/judgment-distillation.md`](../docs/judgment-distillation.md)。

**4つの規律**

1. **追記専用・上書き禁止。** 間違った記帳は消さず、`revises: D-...` を付けた新エントリで訂正する（イベントソーシング）。
2. **verbatim（実発話）を残す。** `quote:` は承認者の原文をコピペで。要約しない。原則に昇格してよいのは quote が引けるものだけ。
3. **推論は昇格させない。** 「たぶんこう考えている」は `principle: [working hypothesis]` タグで台帳に留め、価値判断モデルには上げない。承認者の一言で昇格する。
4. **理由は現場で採取する。** 承認者が理由を言わなければ、差分から `なぜ(仮説):` を推定し、`検証:` 欄に「何が起きたら当たり/外れが確定するか」を書く。事実からあとで理由を導出することはできない（過小決定）ので、判断が起きたそのターンで採取する（→ [現場採取の原理](../docs/judgment-distillation.md)）。

**記帳判定器（1つでも Yes なら記帳）:** ①裁定した ②エージェントを曲げた（却下・訂正=最優先）③金額・締切・対外送信・温度 ④一般則の教訓 ⑤迷い・保留 ⑥前提の重み変化 ⑦委任境界の移動 ⑧待機中の仮説・予測への答え合わせに該当。取らない: 選択肢の無い作業指示・進捗確認・雑談。

書式: 見出し1行で意味が取れること（`grep '^## D-'` で判断一覧が出る）。必須は「判断」「状態」の2行のみ。裁定・訂正なら `quote` と `principle` も付ける。3行で書けるときは3行でよい（記帳コスト最優先）。

**現場で書いたものと、週次蒸留が後から足したものを見分けられるようにする。** 蒸留が起点のエントリは見出しに種別を添える（`[補記/週次蒸留]`＝記帳漏れの補記／`[答え合わせ/週次蒸留]`＝待機中の仮説の判定）。**補記には quote だけを載せ、「なぜ(仮説)」は `[現場採取漏れ]` と書いて空ける**——現場の弁別文脈が失われた後に理由を書くのは導出ではなく捏造だ（蒸留は仮説を発明しない）。この区別があると、「現場採取がどれだけ漏れているか」を後から数えられる。

---

## 判断イベント（新しい順・追記のみ）

<!-- ここは空で出荷される。1件目はあなたの現場の裁定か訂正だ。書式は下のひな型。 -->

---

<!--
新しい判断を追記するときのひな型（コピーして先頭付近に置く）:

### D-YYYY-MM-DD-NN [topic: 〇〇] 判断=＜見出しにも要約を重複（grep用）＞
- 判断: ＜1行＞                              ← 必須
- quote: 「＜承認者の実発話。裁定・訂正の原文1行。要約しない＞」   ← 裁定・訂正なら必須
- 根拠: ＜承認者が理由を明言したなら、その理由＞          ← 任意
- なぜ(仮説): ＜理由が明言されなかったとき、差分から推定した理由。[working hypothesis]扱い・原則に上げない・候補は併記可＞  ← 任意
- 検証: ＜このなぜ(仮説)の当たり/外れが何で確定するか。型=即時／反復／曝露待ち＞  ← なぜ(仮説)を書いたら必須
- 代替案: ＜捨てた案と理由＞                 ← 任意
- 迷い: ＜リスク・保留＞                      ← 任意
- principle: P7 / NEW候補 / [working hypothesis] / なし  ← 価値判断モデルとの紐づけ
- revises: D-YYYY-MM-DD-NN                    ← 訂正イベントのときだけ。旧エントリは消さない
- 状態: 自動実行 / 承認キュー / 裁定済み / 観測記録   ← 必須
- 結果予測: ＜この判断が外部世界にもたらす結果の予測＞   ← 任意（承認≠結果の良さ。金額・対外送信・不可逆ほど記入）
- 実結果: ＜判定期限後に判明した実際の外部結果＞         ← 任意
- 判定期限: ＜実結果を判定する期日＞                     ← 任意
- 撤回: ＜外部結果が予測と乖離し判断を撤回した記録＞     ← 任意

topic は統制語彙（短い固定名）。表記を揺らさない。
月初に前月分を journal_archive/ へ切り出し、当月ファイル冒頭に未決分だけ転記する。
承認キューで却下・修正したものは、事実が変わるなら decisions.md も更新し、
「どう判断したか」はここに1件残して週次蒸留の種にする。
-->
